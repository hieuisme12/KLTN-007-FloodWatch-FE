# Sao chép thành mobile/.env (không commit)
# URL backend — điện thoại phải truy cập được (không dùng localhost trừ khi dùng IP LAN máy dev)
EXPO_PUBLIC_API_BASE_URL=https://api.floodsight.id.vn

# Tuỳ chọn: nền Mapbox cho react-native-maps (Expo Go). Không có → bản đồ OS mặc định.
# EXPO_PUBLIC_MAPBOX_TOKEN=pk....
